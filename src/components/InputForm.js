import styled from 'styled-components';

const InputForm = () => {
  return (
    <Container>
      <h1>hi</h1>
    </Container>
  );
};

const Container = styled.div`
  display: flex;
  flex-direction: row;
  height: 50px;
  align-self: end;
`;
export default InputForm;
